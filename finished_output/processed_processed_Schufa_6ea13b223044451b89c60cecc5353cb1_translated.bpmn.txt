 Upon receiving a Schufa request, the system initiates a forward delay process to handle the request. Concurrently, a scoring request is received, which triggers the perform scoring 1 process. The result of this scoring process is then shared with ExclusiveGateway0, which in turn answers the scoring request.

Meanwhile, the Schufa request also triggers the sharing of results with ExclusiveGateway0. The forward delay process eventually leads to the perform scoring 2 process, which receives the result OK? and shares it with ExclusiveGateway2. This gateway then answers the scoring request and shares the result with ExclusiveGateway2.

The scoring request is also shared with ExclusiveGateway2, which answers the request and shares the result with ExclusiveGateway0. The result is then shared with ExclusiveGateway2, which answers the scoring request.

In summary, the system processes a Schufa request by initiating a forward delay, performing scoring 1, and sharing results with ExclusiveGateway0. The scoring request is also shared with ExclusiveGateway2, which answers the request and shares the result with ExclusiveGateway0. The result is then shared with ExclusiveGateway2, which answers the scoring request.